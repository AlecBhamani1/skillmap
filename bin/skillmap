#!/usr/bin/env python3
"""skillmap launcher — runs the CLI from a source checkout without installation."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from skillmap.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
